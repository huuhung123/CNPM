extends ../layouts/common.pug

block content
    h1.text-center UPDATE SERVICE
    p.text-center ID: #{service._id}

    form(action="/service/update" + service._id, method="POST")
        .form-group 
            label Name:
            input.form-control(type="text", name="name", value="")

        .form-group 
            label Service:
            input.form-control(type="text", name="service", value="")
    
        .form-group
            label Phone:
            input.form-control(type="text", name="phone", value="")

        .form-group
            label Place:
            input.form-control(type="text", name="place", value="")
        button.btn.btn-primary Update